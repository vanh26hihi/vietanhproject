import { Canvas } from "@react-three/fiber";
import { Grid, OrbitControls, PerspectiveCamera, TransformControls } from "@react-three/drei";
import * as THREE from "three";
import { STLExporter } from "three/examples/jsm/exporters/STLExporter.js";
import { FontLoader, type Font } from "three/examples/jsm/loaders/FontLoader.js";
import { TextGeometry } from "three/examples/jsm/geometries/TextGeometry.js";
import { useEffect, useMemo, useRef, useState } from "react";
import { Download, Eye, EyeOff, Lock, RotateCcw, Unlock } from "lucide-react";
import { buildKeycap } from "./geometry";
import { DEFAULT_PARAMS, PROFILES, SIZES, applyProfile, applySize, type KeycapParams, type ProfileId, type SizeId } from "./params";

type Vec3=[number,number,number];
type Mode="translate"|"rotate"|"scale";
type ObjectId="body"|"stem"|"stabL"|"stabR"|"legend";
type ObjState={visible:boolean;locked:boolean;color:string;position:Vec3;rotation:Vec3;scale:Vec3};
const LABEL:Record<ObjectId,string>={body:"Body / vỏ keycap",stem:"Stem giữa",stabL:"Stabilizer trái",stabR:"Stabilizer phải",legend:"Legend / chữ"};
const O0:Record<ObjectId,ObjState>={body:{visible:true,locked:false,color:"#46556d",position:[0,0,0],rotation:[0,0,0],scale:[1,1,1]},stem:{visible:true,locked:false,color:"#1683d8",position:[0,0,0],rotation:[0,0,0],scale:[1,1,1]},stabL:{visible:true,locked:false,color:"#1683d8",position:[0,0,0],rotation:[0,0,0],scale:[1,1,1]},stabR:{visible:true,locked:false,color:"#1683d8",position:[0,0,0],rotation:[0,0,0],scale:[1,1,1]},legend:{visible:true,locked:false,color:"#f7f7f8",position:[0,0,0],rotation:[0,0,0],scale:[1,1,1]}};
const cloneState=()=>structuredClone(O0) as Record<ObjectId,ObjState>;
function sliceGeo(g:THREE.BufferGeometry,start:number,count:number){const a=g.getAttribute("position").array as Float32Array,out=new Float32Array(a.slice(start*3,(start+count)*3)),x=new THREE.BufferGeometry();x.setAttribute("position",new THREE.BufferAttribute(out,3));x.computeVertexNormals();return x;}
function splitStems(p:KeycapParams,g:THREE.BufferGeometry|null){if(!g||!p.stemEnabled)return{} as Partial<Record<ObjectId,THREE.BufferGeometry>>;const n=p.stabEnabled&&p.units>=2?3:1,total=g.getAttribute("position").count,each=Math.floor(total/n),r:Partial<Record<ObjectId,THREE.BufferGeometry>>={stem:sliceGeo(g,0,each)};if(n===3){r.stabR=sliceGeo(g,each,each);r.stabL=sliceGeo(g,each*2,total-each*2)}return r;}
function makeLegend(font:Font|null,text:string,size:number,depth:number,p:KeycapParams){if(!font||!text.trim())return null;const g=new TextGeometry(text,{font,size,depth,curveSegments:10,bevelEnabled:false});g.computeBoundingBox();const b=g.boundingBox!;g.translate(-(b.min.x+b.max.x)/2,-(b.min.y+b.max.y)/2,Math.max(0,p.height-p.dishDepth)+.10);return g.toNonIndexed();}
function applyState(g:THREE.BufferGeometry,s:ObjState){const c=g.clone(),m=new THREE.Matrix4(),q=new THREE.Quaternion().setFromEuler(new THREE.Euler(...s.rotation.map(THREE.MathUtils.degToRad) as Vec3));m.compose(new THREE.Vector3(...s.position),q,new THREE.Vector3(...s.scale));c.applyMatrix4(m);return c;}
function downloadSTL(objects:Partial<Record<ObjectId,THREE.BufferGeometry>>,state:Record<ObjectId,ObjState>){const root=new THREE.Group();for(const id of Object.keys(objects) as ObjectId[]){const g=objects[id],s=state[id];if(!g||!s.visible)continue;root.add(new THREE.Mesh(applyState(g,s),new THREE.MeshBasicMaterial()))}root.updateMatrixWorld(true);const v=new STLExporter().parse(root,{binary:true}) as DataView,blob=new Blob([new Uint8Array(v.buffer,v.byteOffset,v.byteLength)],{type:"model/stl"}),u=URL.createObjectURL(blob),a=document.createElement("a");a.href=u;a.download="keycap-custom.stl";a.click();setTimeout(()=>URL.revokeObjectURL(u),1000);}
function N({l,v,set,step=.1,min,max}:{l:string;v:number;set:(n:number)=>void;step?:number;min?:number;max?:number}){return <label className="grid grid-cols-[1fr_90px] items-center gap-2 text-[11px]"><span>{l}</span><input className="h-8 rounded-md border bg-background px-2 text-right font-mono" type="number" value={v} step={step} min={min} max={max} onChange={e=>set(Number(e.target.value))}/></label>}
function V({title,v,set}:{title:string;v:Vec3;set:(v:Vec3)=>void}){return <div className="rounded-md border p-2"><div className="mb-2 text-[11px] font-semibold">{title}</div><div className="grid grid-cols-3 gap-1">{[0,1,2].map(i=><label key={i} className="text-[10px] text-muted-foreground">{"XYZ"[i]}<input className="mt-1 h-7 w-full rounded border bg-background px-1 text-right font-mono text-[10px]" type="number" value={Number(v[i].toFixed(3))} step={title==="Scale"?.05:.1} onChange={e=>{const a=[...v] as Vec3;a[i]=Number(e.target.value);set(a)}}/></label>)}</div></div>}
function ObjMesh({g,s,selected,mode,onSelect,onPatch}:{g:THREE.BufferGeometry;s:ObjState;selected:boolean;mode:Mode;onSelect:()=>void;onPatch:(x:Partial<ObjState>)=>void}){const ref=useRef<THREE.Mesh>(null);const mesh=<mesh ref={ref} geometry={g} position={s.position} rotation={s.rotation.map(THREE.MathUtils.degToRad) as Vec3} scale={s.scale} onClick={e=>{e.stopPropagation();onSelect()}}><meshStandardMaterial color={selected?"#f59e0b":s.color} roughness={.42}/></mesh>;if(!selected||s.locked)return mesh;return <TransformControls mode={mode} space="world" onMouseUp={()=>{const m=ref.current;if(!m)return;onPatch({position:m.position.toArray() as Vec3,rotation:[THREE.MathUtils.radToDeg(m.rotation.x),THREE.MathUtils.radToDeg(m.rotation.y),THREE.MathUtils.radToDeg(m.rotation.z)],scale:m.scale.toArray() as Vec3})}}>{mesh}</TransformControls>}
function Scene({objects,state,selected,setSelected,mode,patch}:{objects:Partial<Record<ObjectId,THREE.BufferGeometry>>;state:Record<ObjectId,ObjState>;selected:ObjectId;setSelected:(x:ObjectId)=>void;mode:Mode;patch:(id:ObjectId,x:Partial<ObjState>)=>void}){return <Canvas shadows><color attach="background" args={["#eef2f7"]}/><PerspectiveCamera makeDefault position={[28,-34,26]} fov={36} up={[0,0,1]}/><OrbitControls makeDefault target={[0,0,5]}/><ambientLight intensity={.9}/><directionalLight position={[25,-30,40]} intensity={1.6}/>{(Object.keys(objects) as ObjectId[]).map(id=>{const g=objects[id],s=state[id];return g&&s.visible?<ObjMesh key={id} g={g} s={s} selected={selected===id} mode={mode} onSelect={()=>setSelected(id)} onPatch={x=>patch(id,x)}/>:null})}<Grid args={[200,200]} rotation={[Math.PI/2,0,0]} cellSize={1} sectionSize={10} infiniteGrid fadeDistance={120}/><axesHelper args={[15]}/></Canvas>}
export default function KeycapAdvanced(){const[p,setP]=useState<KeycapParams>(DEFAULT_PARAMS),[font,setFont]=useState<Font|null>(null),[fontError,setFontError]=useState(""),[text,setText]=useState("A"),[textSize,setTextSize]=useState(4),[textDepth,setTextDepth]=useState(.55),[state,setState]=useState<Record<ObjectId,ObjState>>(cloneState),[selected,setSelected]=useState<ObjectId>("body"),[mode,setMode]=useState<Mode>("translate");useEffect(()=>{fetch("https://threejs.org/examples/fonts/helvetiker_regular.typeface.json").then(r=>{if(!r.ok)throw new Error(String(r.status));return r.json()}).then(j=>setFont(new FontLoader().parse(j))).catch(()=>setFontError("Không tải được font vector. Kiểm tra mạng rồi tải lại trang."))},[]);const build=useMemo(()=>buildKeycap(p),[p]),stemParts=useMemo(()=>splitStems(p,build.stems),[p,build.stems]),legend=useMemo(()=>makeLegend(font,text,textSize,textDepth,p),[font,text,textSize,textDepth,p]),objects=useMemo(()=>({body:build.shell,...stemParts,...(legend?{legend}:{})}) as Partial<Record<ObjectId,THREE.BufferGeometry>>,[build.shell,stemParts,legend]);const patch=(id:ObjectId,x:Partial<ObjState>)=>setState(s=>({...s,[id]:{...s[id],...x}}));const set=<K extends keyof KeycapParams>(k:K,v:KeycapParams[K])=>setP(x=>({...x,[k]:v}));const ids=(Object.keys(objects) as ObjectId[]).filter(id=>objects[id]);return <div className="grid h-full min-h-0 grid-cols-[310px_1fr_320px] bg-background"><aside className="space-y-3 overflow-y-auto border-r p-3 pt-16"><section className="rounded-lg border bg-card p-3"><h2 className="text-sm font-semibold">Keycap Advanced</h2><p className="mt-1 text-[10px] text-muted-foreground">Vỏ, stem, stabilizer và chữ là object riêng; mọi object có eye/color/lock/XYZ.</p></section><section className="rounded-lg border bg-card p-3"><h3 className="mb-2 text-xs font-semibold">Size / Profile</h3><div className="grid grid-cols-4 gap-1">{(Object.keys(SIZES) as Exclude<SizeId,"custom">[]).map(id=><button key={id} onClick={()=>setP(x=>applySize(x,id))} className={`rounded border px-1 py-1 text-[10px] ${p.sizeId===id?"bg-primary text-primary-foreground":""}`}>{SIZES[id].label}</button>)}</div><div className="mt-2 grid grid-cols-5 gap-1">{(Object.keys(PROFILES) as ProfileId[]).map(id=><button key={id} onClick={()=>setP(x=>applyProfile(x,id))} className={`rounded border px-1 py-1 text-[10px] ${p.profileId===id?"bg-primary text-primary-foreground":""}`}>{PROFILES[id].label}</button>)}</div></section><section className="space-y-2 rounded-lg border bg-card p-3"><h3 className="text-xs font-semibold">Body / hình học</h3><N l="Pitch" v={p.pitch} step={.05} set={n=>set("pitch",n)}/><N l="Rộng đáy" v={p.bottomWidth} set={n=>set("bottomWidth",n)}/><N l="Sâu đáy" v={p.bottomDepth} set={n=>set("bottomDepth",n)}/><N l="Rộng top" v={p.topWidth} set={n=>set("topWidth",n)}/><N l="Sâu top" v={p.topDepth} set={n=>set("topDepth",n)}/><N l="Chiều cao" v={p.height} set={n=>set("height",n)}/><N l="Dày thành" v={p.wallThickness} step={.05} set={n=>set("wallThickness",n)}/><N l="Dày nóc" v={p.topThickness} step={.05} set={n=>set("topThickness",n)}/><N l="Side draft" v={p.sideDraft} step={.5} set={n=>set("sideDraft",n)}/><N l="Bo góc đáy" v={p.cornerRadius} step={.1} set={n=>set("cornerRadius",n)}/><N l="Bo góc top" v={p.topCornerRadius} step={.1} set={n=>set("topCornerRadius",n)}/><N l="Dish depth" v={p.dishDepth} step={.05} set={n=>set("dishDepth",n)}/><N l="Dish strength" v={p.dishStrength} step={.1} set={n=>set("dishStrength",n)}/><N l="Top tilt" v={p.topTilt} step={.5} set={n=>set("topTilt",n)}/><N l="Fillet top" v={p.filletTop} step={.05} set={n=>set("filletTop",n)}/></section><section className="space-y-2 rounded-lg border bg-card p-3"><div className="flex items-center justify-between"><h3 className="text-xs font-semibold">Stem / stabilizer</h3><input type="checkbox" checked={p.stemEnabled} onChange={e=>set("stemEnabled",e.target.checked)}/></div><N l="Stem Ø" v={p.stemDiameter} step={.05} set={n=>set("stemDiameter",n)}/><N l="Stem cao" v={p.stemHeight} step={.05} set={n=>set("stemHeight",n)}/><N l="Cross dài" v={p.crossLength} step={.05} set={n=>set("crossLength",n)}/><N l="Cross rộng" v={p.crossWidth} step={.05} set={n=>set("crossWidth",n)}/><N l="Clearance" v={p.stemClearance} step={.01} set={n=>set("stemClearance",n)}/>{p.units>=2&&<label className="flex items-center justify-between text-[11px]"><span>Stabilizer</span><input type="checkbox" checked={p.stabEnabled} onChange={e=>set("stabEnabled",e.target.checked)}/></label>}{p.units>=2&&p.stabEnabled&&<N l="Stab spacing" v={p.stabSpacing} step={.1} set={n=>set("stabSpacing",n)}/>}</section><section className="space-y-2 rounded-lg border bg-card p-3"><h3 className="text-xs font-semibold">Bù in 3D</h3><N l="XY compensation" v={p.xyCompensation} step={.01} set={n=>set("xyCompensation",n)}/><N l="Z compensation" v={p.zCompensation} step={.01} set={n=>set("zCompensation",n)}/><N l="Shrinkage %" v={p.shrinkage} step={.05} set={n=>set("shrinkage",n)}/><N l="Nozzle" v={p.nozzle} step={.1} set={n=>set("nozzle",n)}/></section></aside><main className="relative min-h-0 pt-12"><div className="absolute left-1/2 top-14 z-10 flex -translate-x-1/2 gap-1 rounded-lg border bg-card/90 p-1 shadow">{([['translate','W · Move'],['rotate','E · Rotate'],['scale','R · Scale']] as [Mode,string][]).map(([m,l])=><button key={m} onClick={()=>setMode(m)} className={`rounded px-3 py-1.5 text-[10px] ${mode===m?"bg-primary text-primary-foreground":""}`}>{l}</button>)}</div><Scene objects={objects} state={state} selected={selected} setSelected={setSelected} mode={mode} patch={patch}/></main><aside className="space-y-3 overflow-y-auto border-l p-3 pt-16"><section className="rounded-lg border bg-card p-3"><h3 className="mb-2 text-xs font-semibold">Legend / chữ vector</h3><input value={text} maxLength={20} onChange={e=>setText(e.target.value)} placeholder="A / ESC / ENTER..." className="h-9 w-full rounded-md border bg-background px-2 text-sm"/><div className="mt-2 space-y-2"><N l="Cỡ chữ" v={textSize} step={.1} set={setTextSize}/><N l="Độ nổi" v={textDepth} step={.05} set={setTextDepth}/></div>{!font&&!fontError&&<p className="mt-2 text-[10px] text-muted-foreground">Đang tải font vector…</p>}{fontError&&<p className="mt-2 text-[10px] text-red-600">{fontError}</p>}<p className="mt-2 text-[10px] leading-relaxed text-muted-foreground">Legend dùng outline font thật, không còn kiểu pixel 5×7. Đây là mesh 3D riêng và được export STL khi đang bật.</p></section><section className="rounded-lg border bg-card p-3"><div className="mb-2 flex items-center justify-between"><h3 className="text-xs font-semibold">Objects</h3><span className="text-[10px] text-muted-foreground">{ids.length}</span></div><div className="space-y-1">{ids.map(id=><div key={id} onClick={()=>setSelected(id)} className={`grid cursor-pointer grid-cols-[28px_28px_1fr_28px] items-center gap-1 rounded-md border p-1 ${selected===id?"border-primary bg-primary/5":""}`}><button onClick={e=>{e.stopPropagation();patch(id,{visible:!state[id].visible})}} className="grid size-7 place-items-center">{state[id].visible?<Eye className="size-3.5"/>:<EyeOff className="size-3.5"/>}</button><input type="color" value={state[id].color} onClick={e=>e.stopPropagation()} onChange={e=>patch(id,{color:e.target.value})} className="size-6"/><span className="truncate text-[10px]">{LABEL[id]}</span><button onClick={e=>{e.stopPropagation();patch(id,{locked:!state[id].locked})}} className="grid size-7 place-items-center">{state[id].locked?<Lock className="size-3.5"/>:<Unlock className="size-3.5"/>}</button></div>)}</div></section><section className="space-y-2 rounded-lg border bg-card p-3"><h3 className="text-xs font-semibold">Transform: {LABEL[selected]}</h3><V title="Position" v={state[selected].position} set={v=>patch(selected,{position:v})}/><V title="Rotation" v={state[selected].rotation} set={v=>patch(selected,{rotation:v})}/><V title="Scale" v={state[selected].scale} set={v=>patch(selected,{scale:v})}/><button onClick={()=>patch(selected,structuredClone(O0[selected]))} className="flex h-8 w-full items-center justify-center gap-1 rounded border text-[10px]"><RotateCcw className="size-3"/>Reset object</button></section>{build.analysis.warnings.length>0&&<section className="rounded-lg border border-amber-300 bg-amber-50 p-3 text-[10px] text-amber-900">{build.analysis.warnings.map(x=><div key={x}>• {x}</div>)}</section>}<button onClick={()=>downloadSTL(objects,state)} className="flex h-9 w-full items-center justify-center gap-2 rounded-md bg-primary text-xs font-medium text-primary-foreground"><Download className="size-4"/>Xuất STL các object đang bật</button></aside></div>}
